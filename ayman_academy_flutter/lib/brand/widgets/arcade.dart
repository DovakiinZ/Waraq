/// Barrel for the Waraq arcade widget set.
///
/// `import 'package:ayman_academy_app/brand/widgets/arcade.dart';` gives a
/// screen the tokens (`context.arc`) and every primitive in one line, which is
/// what the screens actually need — importing the four files separately at 30
/// call sites was the main reason arcade styling drifted.
library;

export 'package:ayman_academy_app/brand/arcade.dart';
export 'arcade_button.dart';
export 'arcade_surfaces.dart';
export 'brand_logo.dart';
